# HANDOFF — 2026-07-29 (cold pickup)

Repo: `C:\work\baseline` (the unified repo — comfy/lumberjacks checkout roots are retired archives).
Audience: any agent (Claude, Codex, other) or Derek, with zero conversation context. All facts below were verified in the 2026-07-28/29 session; paths spot-checked 2026-07-29.

---

## STATE (refreshed 2026-07-29, post-builders)

- **The repo is PUBLIC** (`gh repo view djcdevelopment/baseline` → PUBLIC, flipped 2026-07-29).
  The old "site-downloads-only / baseline stays private" decision is dead; ladder stage 3
  ("Contributor" — renamed from Steward) grants **commit** access, the source was always
  readable once public. `ENDtoEND.txt` is untracked with ZERO git history — verified absent
  from the public repo.
- **Working tree CLEAN, everything committed** — `docs/audit/` was committed at `92445fb`
  per Derek's call (four files: 36h audit, GCP burn review, onboarding review brief +
  annotated results). Run `git log --oneline -15` for current SHAs; history may be rewritten
  by the background automation (see `AGENTS.md`) — never fight it.
- **P7 VM is TERMINATED — stopped since 2026-07-25T23:44 PT, never restarted.** The whole
  live site (roadmap, /join, /community) has been DOWN since then. Deliberate lean posture;
  current burn ≈ the ~$14/mo storage+IP floor. **The deploy session must START the VM
  first** (`gcloud compute instances start comfy-lumberjacks-p7 --project=lumberjacks-exp-20260711-djc --zone=us-west1-b`,
  Derek runs it), wait for `/health` + "Game server connected" (~1 min world reload).
- **Community Workbench: LIVE ON THE AM4 LOCAL LANE; P7 deploy deferred to the lean-GCP
  step.** Stabilization runs on local hardware per Derek: AM4 hosts the Gateway
  (`lj-workbench` container, image `lumberjacks-gateway:m31-workbench-20260729-r2`, bound
  to the tailnet IP + loopback on :4000, mount `/srv/lumberjacks/roadmap` →
  `/var/lib/lumberjacks/roadmap` with the workbench html + downloads env pattern).
  **Loop confirmation 2026-07-29 (all verified from OMEN over the tailnet):** served
  page hash === local render === `X-Workbench-Sha256`; both zip downloads hash-exact with
  correct `X-Download-Sha256`; a zip downloaded through the wire cold-starts (harvest →
  render, corrected config path present); the telemetry kit's own poller reads the live
  v0 API (sim ticking at 20 Hz); all nav pages 200; `/ops/boundary` correctly 403s from
  a non-operator vantage; `/join` and `/health` 200. Publish script's pointer key fixed
  to `downloads` (the endpoint 503s on `tools`). Cost/cycle levers for the later GCP
  step staged in `infra/gcp/p7/RUNBOOK-cost-and-cycle.md`.
- **Discord forum LIVE on the new server** (`1531911987074957442`, invite
  `https://discord.gg/TSHTD38yV`): `#workbench` forum channel with the 8-tag taxonomy,
  "How this works" pinned, "Recoverable pieces" posted. The four tool threads are HELD
  until `/workbench` deploys (their seeds carry site URLs). Provisioning bot:
  `tools/workbench/discord/workbench_discord.py` (config-as-code, structure-only,
  announcement on a hardcoded denylist); state: `tools/workbench/discord/provision-state.json`.
- **PRs are OPEN to anyone; Derek is the sole approval gate** (CONTRIBUTING.md). CLA-vs-DCO
  instrument still unpicked — needed before the first substantial external PR.
- **Networking lane: PAUSED** under a hard-hold pin (see below). No human Steam test is
  scheduled — avoiding them is the point.

## WHAT SHIPPED TODAY (2026-07-28/29)

1. **Networking lane paused, Codex's stranded work landed truthfully**
   - CRE-E08 experiment + harness: `fieldlab/experiments/creative-runtime/cre-e08-adaptive-presentation-replay/`
   - Harmony patch policy: `fieldlab/docs/harmony-patch-policy.md`
   - Patchload A/B instrumentation, labeled honestly "built, never run": `fieldlab/experiments/patchload-ab/` + runbook `fieldlab/docs/runbook-patchload-ab-benchmark.md`
   - Session retro: `fieldlab/retro/SESSION-RETRO-2026-07-28.md`
   - Hard-hold pin: `fieldlab/PINNED-networking-lane-2026-07.md` — resume path is `tools\wave0\Test-Wave0Prelive.ps1` (regenerates the return packet).

2. **Community Workbench catalog surface**
   - `/workbench` page + `/workbench/downloads` routes in Game.Gateway; built + tested in the `mcr.microsoft.com/dotnet/sdk:9.0` container.
   - Data-driven: `Lumberjacks/docs/workbench/workbench.json` (7 tools, 5-stage ladder, generator-enforced honesty invariants) → `Lumberjacks/scripts/workbench.mjs` → generated `Lumberjacks/src/Game.Gateway/Community/workbench.html`.
   - Roadmap milestone **A7** added; nav links added across all Community pages.

3. **Two cold-start zips** in `tools/workbench/dist/` (**gitignored** — they exist on this machine only):
   - `quest-picker.zip` — sha256 `d0b53016cbce5febb6f35d24b581a2112298f3650d48cf3190e44ece7ac4b65a`, 36335 B
   - `telemetry-starter.zip` — sha256 `bf065eb47da01f3305ec10c4c6b8ec03d99329cf6a5dc02c2ba5f26eded32211`, 8485 B
   - These hashes are **baked into workbench.json access blocks**. Rebuild recipe is in RESUME RECIPES below; a rebuild changes hashes and forces a workbench.json update + re-render, or the publish script refuses to upload.

4. **Discord layer as FILES ONLY — nothing has been posted**
   - `Lumberjacks/docs/workbench/discord/`: `00-announcement.md` (placeholders `<WORKBENCH-URL>` `<ONEPAGER-URL>` `<ACCESS-URL>` — fill only AFTER deploy, with `https://comfy-p7.duckdns.org/workbench` etc.), thread seeds `01`–`06`, `07-forum-tags-setup.md`, `08-saved-replies.md`, `09-discord-bot-setup.md`, `drafts/2026-07-29-announcement.md` (output of the new drafter).
   - Ownership ledger: `Lumberjacks/docs/workbench/OWNERS.md` (note: sits one level ABOVE discord/), all roles unclaimed.

5. **Cognitive-lift portfolio** — `plans/cognitive-lift-portfolio.md`: 13 researched + sourced ideas, weighted decision matrix, **top 5 built**:
   - Announcement drafter `tools/workbench/new_announcement_draft.py` — self-test green 11/11.
   - Feedback distiller `tools/workbench/distill_feedback.py` — self-test 15/15; **activates only after Derek creates the read-only Discord bot** per the 09 doc.
   - Forum tags doc (07), saved replies (08).
   - Bug-fix-first-task lens + "Already answered" section in `Lumberjacks/docs/workbench/TEMPLATE-one-pager.md`.
   - Both scripts are deterministic / LLM-free **by design** (HEARTH was unreliable this session: gcp-gemini-pro truncated ~95 tok, omen-ollama cold — see memory notes). Don't "improve" them by adding LLM calls.

6. **Discord provisioning bot** — `tools/workbench/discord/`, added 2026-07-29 for the new
   server. Config-as-code: the forum channel, the 8-tag taxonomy (parsed live out of the 07
   doc, not duplicated), the guidelines box and the six posts all come from repo files, and
   `apply` converges instead of duplicating. Structure only, by construction — no free-text
   message path, `allowed_mentions` empty on every write, `00-announcement.md` on a
   hardcoded denylist. `export` replaces the DiscordChatExporter step feeding
   `distill_feedback.py`. Standard library, no daemon, no HEARTH. Nothing posted yet.

### Fixes landed worth knowing
- Quest picker + `quest-view-schema.md` pointed at a pruned **comfy-control** config path while the mod actually reads **comfy-network-sense** — fixed; catalogs now under `recipes/quest-catalogs/`.
- Licensing wording: public journal said "open source" — corrected append-only, glossary entry added, and `roadmap.mjs` now has a guard that **FAILS any new note/JSON matching `/open[- ]source/i`**. Correct phrase: **"public source (BSL 1.1)"**.
- Steam-join has **NO rate limiting** yet — the runbook states this truthfully; don't claim otherwise.
- `ComfyStewardView` `LICENSE.md` is **proprietary/paid** — the catalog says so; whether to relicense is a pending Derek decision.

## PENDING BY ACTOR

### Derek (full detail in `DEREK-BATCH-1.md`, ~90 min total)
1. Approve `workbench.html` content (review the generated page).
2. Four decisions: (a) commit the `docs/audit/` memos — includes the **unread GCP burn memo, ~$95–115/mo**; (b) comfy public data; (c) StewardView license; (d) roadmap links pointing at a private repo → 404.
3. Discord session on the NEW server (`1531911987074957442`): ~10 min of Developer-Portal setup, then approve the dry-run receipt and let `tools/workbench/discord/workbench_discord.py` provision the forum. **Do NOT post 00 yet** (now enforced in code). Thread URLs no longer need pasting — `apply` writes them to `provision-state.json`. Seeds 01–04 are blocked until `/workbench` is live, so the four tool threads land after the deploy.
4. Deploy go/no-go.
5. After deploy: post the announcement (00 with placeholders filled).

### Next agent (when Derek returns with input)
1. **Thread-URL fill**: read `tools/workbench/discord/provision-state.json` (written by the provisioning bot's `apply`; each entry maps thread URL → `workbench_tools` ids) into `discussion.href` per tool in `Lumberjacks/docs/workbench/workbench.json` → `npm run workbench:render` + `npm run workbench:check` → commit via ceremony below.
2. **Deploy batch (Derek present)** — see RESUME RECIPES.
3. **NEVER `terraform apply` from this repo** — it would destroy the live VM + 4 live resources (RECONCILE-GAP open; active TF state lives in the retired comfy checkout).
4. After the first deploy, content-only updates = `Publish-WorkbenchAssets.ps1` alone (no image rebuild).
5. Fill announcement placeholders only after deploy; new batch-cycle drafts via `python tools/workbench/new_announcement_draft.py` (state: `tools/workbench/announcement-state.json`).

## PRODUCT BOUNDARY (read before packaging anything)

**Baseline is the community toolkit; HEARTH/Mechnet is the operator's personal AI lab —
they never ship together.** No `mcp__hearth__*`, `127.0.0.1:8710`, or
`C:\work\commandcenter` anything in community-facing zips/pages/docs. Community
automation must run without HEARTH (the two new scripts are LLM-free by design). The
`network/mcp` Comfy gateway is Baseline's own dev tool — a different thing. Full
statement + rationale: `docs/baseline-vision-and-boundary.md` (Derek's canonical product
framing, 2026-07-29 — read it before writing any outward copy).

## CEREMONY & GOTCHAS (any future commit)

- **Roadmap note ceremony**, from `Lumberjacks/`:
  1. `npm run roadmap:note -- --milestone <M> --kind <k> --author <you> --summary "..." --impact "..."`
  2. Stage your changes AND always include: `docs/roadmap/commit-notes.jsonl`, `docs/roadmap/valheim-volunteer-roadmap.json`, `src/Game.Gateway/Community/roadmap.html`
  3. `npm run roadmap:check -- --staged` must pass before committing.
- **Workbench edits**: edit `workbench.json` only, then `npm run workbench:render` + `npm run workbench:check`. **Never hand-edit generated HTML** (workbench.html, roadmap.html).
- **Never** bulk-edit files via PowerShell `Get-Content`/`Set-Content` round-trips — PS 5.1 corrupts UTF-8.
- **Builds**: Lumberjacks (net9) builds inside `mcr.microsoft.com/dotnet/sdk:9.0` container; the mod (net48) builds with host dotnet 8 using the `-p:PluginOutputPath=<nonexistent-dir>` guard.
- The roadmap.mjs licensing guard fails commits containing "open source" in new notes/JSON — write "public source (BSL 1.1)".
- Background automation force-pushes main: pull/rebase before working; never force-push.

## KEY FILE INDEX

| Path | What it is |
|---|---|
| `fieldlab/PINNED-networking-lane-2026-07.md` | Hard-hold pin on the networking lane + resume conditions |
| `fieldlab/retro/SESSION-RETRO-2026-07-28.md` | Session retro for the pause decision |
| `DEREK-BATCH-1.md` | Derek's full pending-decision batch (~90 min) |
| `plans/cognitive-lift-portfolio.md` | 13-idea researched portfolio + weighted matrix, top-5 built |
| `Lumberjacks/docs/workbench/workbench.json` | Source of truth: 7 tools, stage ladder, baked zip hashes, discussion.href slots |
| `Lumberjacks/scripts/workbench.mjs` | Generator (render + check, honesty invariants) |
| `Lumberjacks/src/Game.Gateway/Community/workbench.html` | GENERATED — never hand-edit |
| `Lumberjacks/docs/workbench/tools/` | The 7 one-pagers (quest-picker, steward-view, community-telemetry, steam-join, camera-gallery, mcp-mod-channel, quest-submission-bridge) |
| `Lumberjacks/docs/workbench/TEMPLATE-one-pager.md` | One-pager template (bug-fix-first lens, Already-answered section) |
| `Lumberjacks/docs/workbench/OWNERS.md` | Ownership ledger, all unclaimed |
| `Lumberjacks/docs/workbench/discord/` | 00 announcement (placeholders), 01–06 seeds, 07 tags, 08 replies, 09 bot setup, drafts/ |
| `tools/workbench/dist/quest-picker.zip`, `telemetry-starter.zip` | Cold-start zips (gitignored, local-only) |
| `tools/workbench/New-WorkbenchZip.ps1` | Zip builder (runs privacy gate) |
| `tools/workbench/Test-WorkbenchZipPrivacy.ps1` | Mandatory privacy scanner gate |
| `tools/workbench/Publish-WorkbenchAssets.ps1` | Deploy uploader; verifies hashes vs workbench.json, refuses on mismatch |
| `tools/workbench/new_announcement_draft.py` | Deterministic announcement drafter (11/11 self-test) |
| `tools/workbench/distill_feedback.py` | Deterministic feedback distiller (15/15; fed by the provisioning bot's `export`) |
| `tools/workbench/discord/workbench_discord.py` | Discord forum provisioner + content sync + feedback export (64/64 self-test). Structure only — cannot reply, mention, or post the announcement |
| `tools/workbench/discord/provision.json` | Which seed becomes which post; `site_base_url` gates the four link-carrying posts |
| `tools/workbench/discord/provision-state.json` | Written by `apply`: thread ids/URLs for the `discussion.href` fill |
| `tools/workbench/discord/receipts/` | Dry-run receipts for Derek's approval |
| `DECISIONS-PENDING.md` (repo root) | Root pending-decision log |
| `fieldlab/DECISIONS-PENDING.md` | Fieldlab-scoped pending decisions |
| `docs/audit/` (uncommitted) | 36h independent audit + GCP burn-rate review memos |
| `C:\Users\derek\.claude\plans\i-m-running-out-of-jaunty-cupcake.md` | The session plan file this work executed |

## RESUME RECIPES

### A. Thread-URL fill (first thing after Derek's Discord session)
1. Edit `Lumberjacks/docs/workbench/workbench.json`: set `discussion.href` for each of the 6 tools from Derek's thread URLs.
2. From `Lumberjacks/`: `npm run workbench:render` then `npm run workbench:check`.
3. Commit via the roadmap ceremony above (note + 3 roadmap files staged + `roadmap:check --staged`).

### B. Deploy batch (Derek present; go/no-go first)
1. `infra/gcp/p7/scripts/New-GatewayReleaseCut.ps1 -ImageReleaseId <new-id> -AdmittedModRelease m30-rolecontrol-20260723-r1`
2. `infra/gcp/p7/scripts/Promote-GatewayImage.ps1` (gateway is image-pinned; this is the only deploy path — deploy-gateway.ps1 is stale)
3. `tools/workbench/Publish-WorkbenchAssets.ps1` — uploads html + zips + tools.json to `/mnt/comfy-p7/lumberjacks/roadmap/`; verifies zip hashes against workbench.json and refuses on mismatch.
4. Verify `https://comfy-p7.duckdns.org/workbench` returns 200; regression-sweep the existing Community pages and `/join`.
5. Then fill the 00-announcement placeholders (`<WORKBENCH-URL>` → `https://comfy-p7.duckdns.org/workbench`, etc.) and hand to Derek to post.
6. Subsequent content-only updates: step 3 alone.

### C. Zip rebuild (only if zip content must change)
1. `tools\workbench\New-WorkbenchZip.ps1 -Tool <quest-picker|telemetry-starter>` — the privacy scanner `Test-WorkbenchZipPrivacy.ps1` gates it and must pass.
2. New sha256/size → update the tool's access block in `workbench.json`, then `npm run workbench:render` + `npm run workbench:check`.
3. If you skip step 2, `Publish-WorkbenchAssets.ps1` will refuse. That is by design.

### E. Discord forum provisioning (new server, greenfield)
1. One-time: app + token + invite per `Lumberjacks/docs/workbench/discord/09-discord-bot-setup.md`. Token lives at `%USERPROFILE%\.baseline\workbench-discord.token` — never in the repo.
2. `python tools/workbench/discord/workbench_discord.py plan` → read the receipt it writes under `receipts/` → `apply --yes --expect-plan <hash>`.
3. Pre-deploy, seeds 01–04 are blocked on `<ONEPAGER-URL>`/`<ACCESS-URL>`. After recipe B lands, re-run with `--site-base-url https://comfy-p7.duckdns.org` to publish them.
4. Then recipe A (thread-URL fill) reads `provision-state.json`.
5. Feedback later: `export --out ../workbench-exports` then `distill_feedback.py --export-dir ../workbench-exports`.

### D. Networking lane un-pin (do NOT do this casually)
1. Read `fieldlab/PINNED-networking-lane-2026-07.md` in full — it defines the resume conditions. No human Steam test is scheduled and none should be booked; avoiding them is the point of the pin.
2. Resume entry point: `tools\wave0\Test-Wave0Prelive.ps1` regenerates the return packet.
3. Related honest-state markers: patchload A/B is instrumented but has NEVER run; CRE-E08 is a candidate, not a validated result.

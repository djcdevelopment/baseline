# Session Residue: GM B — <guild> _(backfill skeleton — not yet grounded)_

> ⚠️ **Backfill skeleton.** M1-3 asks for one residue note per already-absorbed GM,
> backfilled from memory/handoffs. The repo holds no committed record of these
> sessions (handoffs are sparse), so every GM-specific field below is an unfilled
> placeholder marked `<needs Derek>`. **Do not treat any line here as ground
> truth.** Rename this file to the real guild and fill it from the actual session
> (stream VOD, notes, memory). Until then it exists to prove the template and hold
> the slot. See [../templates/session-residue.md](../templates/session-residue.md).

**Date:** `<needs Derek — session date>`
**GM:** `<needs Derek — name>` / `<needs Derek — guild>`

### Common vs. bespoke
- **Common:** `<needs Derek — the standard onboarding steps every GM goes through>`
- **Bespoke:** `<needs Derek — what's unique to this guild>`

### Vocabulary mapping
| Their term | Our term |
| :--- | :--- |
| `<needs Derek>` | `<needs Derek>` |

### Manual steps I performed
- `<needs Derek — hand-done setup/import/config steps for this GM>`

### Toil surfaced
`<needs Derek — the repetitive pain this GM carries>`

### The one manual thing that should never need doing by hand again
> `<needs Derek — the single biggest time-theft to take off this GM's plate>`
